#include<stdio.h>
#include<conio.h>
int main()
{
int n,r,reverse=0;
printf("enter the number");
scanf("%d",&n);
for(n!=0;n/n=10)
{
r=n%10;
reverse=reverse*10+r;
}
printf("%d reversed number",n);
return 0;
}
